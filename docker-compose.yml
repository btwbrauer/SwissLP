version: '3.8'

services:
  swisslp:
    build: .
    volumes:
      - .:/workspace
      - nix-store:/nix/store
      - nix-var:/nix/var
    environment:
      - NIX_PATH=nixpkgs=channel:nixos-25.05
    stdin_open: true
    tty: true
    working_dir: /workspace

volumes:
  nix-store:
  nix-var:
